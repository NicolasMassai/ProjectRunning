{% extends 'base.html.twig' %}

{% block title %}Hello BankController!{% endblock %}

{% block body %}
<style>
    .required { font-size : 40px;}
    .bankTwig button{  background-color: #4caf50; color: #fff;padding: 8px 12px;border: none;
        border-radius: 4px;cursor: pointer;transition: background-color 0.3s ease;font-size: 22px;}
     .bankTwig button:hover {
    background-color: #45a049;
  }
</style>

   

    <div {{ react_component('header')}}></div>

            {% for message in app.flashes('notification') %}
                    <div class="alert alert-info alert-dismissible fade show">
                        {{ message }}
                    </div>
            {% endfor %}

        <div class='bankTwig'>
            {{ form(form) }}
        </div>

    <div {{ react_component('footer')}}></div>

{% endblock %}