{% extends 'base.html.twig' %}

{% block title %}Hello ProduitController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">

    <ul>
        <form action="{{ path('app_produit_chaussure') }}" method="post">
            <button type="submit" class="btn btn-outline-secondary btn-sm">Chaussure</button>
        </form>
        <br>
    </ul>
     <ul>
        <form action="{{ path('app_produit_montre') }}" method="post">
            <button type="submit" class="btn btn-outline-secondary btn-sm">Montre</button>
        </form>
        <br>
    </ul>

</div>
{% endblock %}
